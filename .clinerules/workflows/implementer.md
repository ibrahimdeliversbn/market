Acting as the implementer, building exactly the scoped task described in this message.

Requirements:
- Only edit the file(s) explicitly mentioned — do not create new files unless
  necessary for this task.
- Follow the existing code style and Tailwind classes already used in the project.
- Keep changes scoped to this task only — do not refactor unrelated code.

When done, use attempt_completion with a clear summary of what was changed.
