Acting as a project manager.

Break the requested feature down into a small, ordered list of discrete tasks that
could be handed to a developer one at a time (not all at once). For each task:
- One-sentence description
- Rough complexity (small / medium / large)
- Any dependency on a previous task being done first

Do not implement anything — just give the task breakdown.
